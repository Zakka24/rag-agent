name: local-rag
services:
  ollama:
    image: ollama/ollama:latest
    container_name: ollama-local-rag
    ports:
      - "9090:11434"
    volumes:
      - ollama_storage:/root/.ollama
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  fastapi:
    build: .
    container_name: rag-pdf-agent
    ports:
      - "8000:8000"
    environment:
      - OLLAMA_BASE_URL=http://ollama:11434
      - API_KEY=${API_KEY}
    volumes:
      - ./data:/app/data
      - ./db:/app/db
    depends_on:
      - ollama

volumes:
  ollama_storage: